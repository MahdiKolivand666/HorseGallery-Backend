import mongoose from 'mongoose';

const MONGODB_URI = 'mongodb://localhost:27017/horsegallery';

// Define Product Schema for script
const ProductSchema = new mongoose.Schema({}, { collection: 'products', strict: false });
const Product = mongoose.model('Product', ProductSchema);

// توضیحات نمونه برای انواع مختلف محصولات (حداقل 5 خط)
const getDescriptionByCategory = (name: string, category: string, subcategory?: string): string => {
  const nameLower = name.toLowerCase();
  
  // تشخیص نوع محصول بر اساس نام
  if (nameLower.includes('گردنبند') || nameLower.includes('necklace')) {
    return `گردنبند طلای زیبا و با کیفیت که با دقت و ظرافت ساخته شده است.
این گردنبند با طراحی کلاسیک و مدرن، مناسب برای استفاده در مناسبت‌های مختلف است.
طلای به کار رفته در این محصول از عیار بالا و کیفیت ممتاز برخوردار است.
این گردنبند می‌تواند هدیه‌ای مناسب برای عزیزان شما باشد.
با خرید این محصول، زیبایی و شکوه را به استایل خود اضافه کنید.`;
  }
  
  if (nameLower.includes('دستبند') || nameLower.includes('bracelet')) {
    return `دستبند طلای ظریف و زیبا که با مهارت و هنر ساخته شده است.
این دستبند با طراحی منحصر به فرد، مناسب برای استفاده روزمره و مناسبت‌های خاص است.
طلای به کار رفته در این محصول از کیفیت بالا و درخشش ممتاز برخوردار است.
این دستبند می‌تواند نمادی از عشق و محبت باشد.
با خرید این محصول، ظرافت و زیبایی را به دستان خود اضافه کنید.`;
  }
  
  if (nameLower.includes('گوشواره') || nameLower.includes('earring')) {
    return `گوشواره طلای زیبا و ظریف که با دقت و هنر ساخته شده است.
این گوشواره با طراحی مدرن و کلاسیک، مناسب برای استفاده در مراسم و مناسبت‌های مختلف است.
طلای به کار رفته در این محصول از عیار بالا و کیفیت عالی برخوردار است.
این گوشواره می‌تواند هدیه‌ای مناسب برای بانوان باشد.
با خرید این محصول، زیبایی و درخشش را به چهره خود اضافه کنید.`;
  }
  
  if (nameLower.includes('آویز') || nameLower.includes('pendant')) {
    return `آویز طلای زیبا و با کیفیت که با ظرافت و هنر ساخته شده است.
این آویز با طراحی منحصر به فرد، مناسب برای استفاده در گردنبندهای مختلف است.
طلای به کار رفته در این محصول از عیار بالا و درخشش ممتاز برخوردار است.
این آویز می‌تواند نمادی از عشق و محبت باشد.
با خرید این محصول، زیبایی و شکوه را به استایل خود اضافه کنید.`;
  }
  
  if (nameLower.includes('انگشتر') || nameLower.includes('ring')) {
    return `انگشتر طلای زیبا و ظریف که با دقت و مهارت ساخته شده است.
این انگشتر با طراحی کلاسیک و مدرن، مناسب برای استفاده در مناسبت‌های مختلف است.
طلای به کار رفته در این محصول از کیفیت بالا و درخشش عالی برخوردار است.
این انگشتر می‌تواند هدیه‌ای مناسب برای عزیزان شما باشد.
با خرید این محصول، ظرافت و زیبایی را به دستان خود اضافه کنید.`;
  }
  
  if (nameLower.includes('پابند') || nameLower.includes('anklet')) {
    return `پابند طلای زیبا و ظریف که با هنر و مهارت ساخته شده است.
این پابند با طراحی مدرن و منحصر به فرد، مناسب برای استفاده در فصل تابستان است.
طلای به کار رفته در این محصول از عیار بالا و کیفیت ممتاز برخوردار است.
این پابند می‌تواند نمادی از زیبایی و ظرافت باشد.
با خرید این محصول، زیبایی و درخشش را به پاهای خود اضافه کنید.`;
  }
  
  if (nameLower.includes('سنجاق') || nameLower.includes('pin') || nameLower.includes('brooch')) {
    return `سنجاق سینه طلای زیبا و با کیفیت که با دقت و ظرافت ساخته شده است.
این سنجاق با طراحی کلاسیک و مدرن، مناسب برای استفاده در لباس‌های رسمی است.
طلای به کار رفته در این محصول از عیار بالا و درخشش ممتاز برخوردار است.
این سنجاق می‌تواند هدیه‌ای مناسب برای بانوان باشد.
با خرید این محصول، زیبایی و شکوه را به استایل خود اضافه کنید.`;
  }
  
  // توضیحات پیش‌فرض (حداقل 5 خط)
  return `جواهر طلای زیبا و با کیفیت که با دقت و هنر ساخته شده است.
این محصول با طراحی منحصر به فرد، مناسب برای استفاده در مناسبت‌های مختلف است.
طلای به کار رفته در این محصول از عیار بالا و کیفیت ممتاز برخوردار است.
این جواهر می‌تواند هدیه‌ای مناسب برای عزیزان شما باشد.
با خرید این محصول، زیبایی و درخشش را به استایل خود اضافه کنید.`;
};

async function updateProductDescriptions() {
  try {
    await mongoose.connect(MONGODB_URI);
    console.log('✅ Connected to MongoDB');

    // دریافت همه محصولات
    const products = await Product.find({}).lean();
    console.log(`📦 Found ${products.length} products`);

    let updatedCount = 0;
    let skippedCount = 0;

    for (const product of products) {
      const productData = product as any;
      const currentDescription = productData.description || '';
      const name = productData.name || 'محصول';
      const category = productData.category || '';
      const subcategory = productData.subcategory || '';

      // اگر توضیحات کمتر از 200 کاراکتر است یا خالی است، توضیحات جدید اضافه کن
      // یا اگر تعداد خطوط کمتر از 5 است
      const lineCount = currentDescription.split('\n').filter(line => line.trim().length > 0).length;
      if (!currentDescription || currentDescription.length < 200 || lineCount < 5) {
        // استفاده از توضیحات خاص بر اساس نوع محصول
        const newDescription = getDescriptionByCategory(name, category, subcategory);

        await Product.findByIdAndUpdate(
          productData._id,
          { $set: { description: newDescription } },
          { new: true },
        ).exec();

        console.log(`✅ Updated: ${name}`);
        updatedCount++;
      } else {
        console.log(`⚠️  Skipped: ${name} (already has description)`);
        skippedCount++;
      }
    }

    console.log(`\n📊 Summary:`);
    console.log(`✅ Updated: ${updatedCount} products`);
    console.log(`⚠️  Skipped: ${skippedCount} products (already have description)`);

    await mongoose.disconnect();
    console.log('✅ Disconnected from MongoDB');
  } catch (error) {
    console.error('❌ Error:', error);
    process.exit(1);
  }
}

updateProductDescriptions().catch(console.error);

